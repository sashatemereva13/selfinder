# Copy to monitoring/.env (gitignored) and fill in real values.
# docker compose reads .env in this directory automatically — no extra
# flag needed.

# Discord webhook URL for Grafana alert notifications. Create one in
# Discord: Server Settings -> Integrations -> Webhooks -> New Webhook.
DISCORD_WEBHOOK_URL=
